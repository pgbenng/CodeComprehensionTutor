export const add = {
  function: (x, y) => {
    // Assume x and y are numbers

    return x + y;
  },
  display:
    'function foo(x, y) {\n  // Assume x and y are numbers\n\n  return x + y;\n}\n',
};
